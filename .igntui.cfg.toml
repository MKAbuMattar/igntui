# igntui.cfg.toml — generated by igntui; safe to commit.
# Re-running igntui in this directory restores this selection.

schema_version = 1
generated_at = "2026-04-27T12:08:07Z"
igntui_version = "0.0.2"

[selection]
templates = [
    "pycharm+all",
    "python",
    "visualstudiocode",
]
search_mode = "fuzzy"

[output]
path = ".gitignore"
preserve_user_edits = true
